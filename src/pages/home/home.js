import React from 'react';
import Header from '../../components/header';
import Login from '../home/login';

const Home = () => {
    return (
        <div>
            <Header/>
            <Login/>
        </div>
    );
}

export default Home;
